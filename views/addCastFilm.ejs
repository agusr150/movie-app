<!DOCTYPE html>
<html>
<head>
<title>Add cast film</title>
</head>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
<%active0 = ""%>
<%active1 = ""%>
<%active2 = "active"%>
<%active3 = ""%>
<%-include('partial/navbar')%>

<body style="margin-left: 40px;">
    
    <form action="/movies/addCast/<%=movie.id%>" method="POST">
        <h1>
        <label for="movie"><%=movie.name %></label> <br>
        </h1>

        <h2>Add Cast</h2>
        <h5>Actor:</h5>
        <select id="cast" name="cast" >
            <%for (let i=0; i<cast.length; i++){%>
                <option><%=cast[i].getName()%></option>
            <%}%>
        </select><br>
        <h5>Role:</h5> 
        <input type="text" id="role" name="role"><br>
        <br>
          <button type="submit" value="Submit">Save</button>
          <button type="reset" value="Reset">Cancel</button>
        
        <table style="width:80%" class="table table-bordered">
            <thead class="thead-light">
            <tr>
                <th>Actor</th>
                <th>Role</th>
            </tr>
            </thead>
            <%if (aktor.length === 0){%>
                <td>no cast yet</td>
                <td>no cast yet</td>
            <%} else {%>
            <%for (let j=0; j<aktor.length; j++){%>
            <tr>
                <td><%=aktor[j].getName() %></td>
                <%castFilm = aktor[j].CastFilm%>
                <td><%=castFilm.role %></td>
                <%}%>
            </tr>
            <%}%>
            
        </table>

      </form> 

</body>
</html> 